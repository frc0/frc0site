# This file was partially auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Build and Deploy To Firebase Hosting On Merge 
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        name: Checkout
      - name: Build Static Files 
        uses: lowply/build-hugo@v0.110.0
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FRCZERO_C27D3 }}'
          channelId: live
          projectId: frczero-c27d3
